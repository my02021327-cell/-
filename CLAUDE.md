# BioGuard-AI — 작업 지침

영천 통합바이오가스화시설(BGP)의 일별 운전데이터로 **메탄생성량 예측**과 **소화조 건강상태
관제**를 수행하는 프로젝트.

## 문서 우선순위

작업 전 아래 순서로 읽는다. **뒤 문서가 앞 문서를 뒤집는 구조**이므로 순서가 중요하다.

| 순위 | 문서 | 역할 |
|:---:|---|---|
| 1 | `docs/EXPERT_REVIEW.md` | **최신 판정.** 설계문서 v2의 치명 결함(A1·A2) 지적 + 신규 정량 기반(B1~B4) |
| 2 | `docs/MODELING_OVERVIEW.md` | 설계 문서 v2. §E 권고 미반영 상태이므로 A1·A2 항목은 검토서 우선 |
| 3 | `docs/reference/영천BGP_AI참조문서.md` | 스키마·DQ-01~12·정량 관계식의 1차 출처 |
| 4 | `docs/INGESTION_REPORT.md` | 위 문서들의 재현 검증 결과 + 문서에 없는 재현 규약 |
| 5 | `docs/reference/literature/` | 1차 문헌 (SAO 경로 전환) |

## 데이터

**정본: `data/영천BGP_MASTER_2018-2023.csv`** (2,086행 × 61열, 2018-01-01 ~ 2023-09-17,
A/B 계열 분리, DQ 플래그 반영).

`data/master.xlsx`·`data/targets.xlsx`는 **구 데이터**다. A/B를 평균으로 합쳐 데이터 결함을
정상 신호처럼 보이게 만든다(설계 §2.1). 신규 작업에 쓰지 말 것.

데이터를 건드린 뒤에는 `python scripts/validate_master.py`(68 검사)를 돌려 회귀를 확인한다.

## 반드시 지킬 규칙

이 규칙들은 전부 데이터로 검증됐다(`docs/INGESTION_REPORT.md`). 어기면 결과가 조용히 틀린다.

1. **persistence를 항상 병기한다.** 바이오가스는 ACF(1)=0.916의 확률보행에 가깝다.
   h=1일은 전일값만으로 R²=0.932다. 절대량 R²는 자기상관을 흡수해 **높아도 착시**다.
   지평별 skill score `1 − MSE_model/MSE_persistence` 를 보고한다.
2. **예측 지평은 h=7·14일이 주 타깃.** h=1은 persistence가 지배해 ML 여지가 없고,
   h=30은 persistence 자체가 무효(R²=−0.564)다. 운영 의사결정도 주 단위다.
3. **무작위 K-fold 금지.** 인접일 누출로 R²가 과대평가된다. 시간순 분할
   (학습 2018~2021 / 검증 2022~2023-09-17) 또는 rolling-origin을 쓴다.
4. **주말 랩 데이터 선형보간 금지.** 랩 항목 주말 측정률 0.3%(주중 88%)로 MCAR이 아니다.
   결측 마스크를 피처로 보존한다. 유량 항목(반입·투입·가스)은 요일 무관 ~100%로 성격이 다르다.
5. **A/B 반응조를 독립 표본으로 취급 금지.** r=0.927의 병렬 동일조건 운전이다.
6. **`FAN > 300` 절대임계를 경보로 쓰지 않는다.** 측정일의 75.1%에서 발화한다. 이 시설은
   순치(acclimated)된 계이고 해당 임계는 비순치 기준이다. FAN은 **2차 원인 판별 변수**다.
   경보는 `Y_COD < 90일 기준선 −15%`(발화율 9.3%)를 쓴다.
7. **VFA/알칼리도를 단독 신호등으로 쓰지 않는다.** 암모니아가 NH₄HCO₃로 분모(알칼리도)를
   부풀려, 성능이 하락하는 동안 지표는 "개선"으로 표시된다(2022년 지표 최저 = 메탄 최저).
8. **정규화 성능 KPI는 `Y_COD`.** VS 기반 수율(`gas_per_VS` 등)은 DQ-08 측정법 계단변화로
   절대값을 신뢰할 수 없다. `in_VS` 대신 `in_TS`(`acid_TS_pct`)를 쓴다.
9. **부하 변수는 `feed_AB_tpd` 하나만.** 반입량은 여액저장조에서 완전 평활화되어 당일
   가스와 r≈0.138이다. 기질별 반입량은 r≈0.00.
10. **피처 제외**: `in_VS`(TS와 공선), `dig_T_A/B_C`(단독 R²=0.001·센서결함), H₂S(고정
    가정치), 개별 VFA 6종(6년간 미측정).

## 계산 규약 (문서에 없어 확정한 것)

```python
# Y_COD — 소화조 COD 는 A·B 평균. A 단독이면 검토서 수치와 어긋난다.
dig_cod = df[["dig_CODcr_A_mgL", "dig_CODcr_B_mgL"]].mean(axis=1)
Y_COD = df.CH4_m3d / (df.feed_AB_tpd * (df.acid_CODcr_mgL - dig_cod) / 1000)

# 연도별 집계·상관 전에 열역학 필터를 먼저 적용한다 (검토서 B3).
valid = df[(Y_COD > 0) & (Y_COD <= 0.35)]

# FAN 재계산 시 온도는 '실측, 센서결함이면 38.0'. 항상 38 대입하면 최대 141 mg/L 어긋난다.
T = df.dig_T_A_C.where(df.flag_TA_sensor_fault == 0).fillna(38.0)
```

- **2018년은 Y_COD 기준선 산출에서 제외한다** — 유효 관측 50건 중 72%가 열역학적으로 불가능.
- **DQ 플래그 구간은 이미 NaN 마스킹돼 있다.** 설계 §3 파이프라인 ① 단계는 무연산이며,
  플래그 열은 "이 결측의 사유 코드"로 쓴다.

## 현행 코드 상태

`src/` 는 위 규칙 이전(v1)에 작성되어 **1·3·4·7·8·9번을 위반**한다(상세: `docs/INGESTION_REPORT.md` §5).
코드 개편 시 설계문서 v2가 아니라 **검토서 §E 반영 후의 v3 기준**으로 진행할 것.

## 실행

```bash
pip install -r requirements.txt
python scripts/validate_master.py   # 데이터셋 수용 검증 (68 검사)
python -m src.train                 # v1 파이프라인 (구 데이터 기준, 개편 대상)
```
