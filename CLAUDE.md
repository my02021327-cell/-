# BioGuard-AI — 작업 지침

영천 통합바이오가스화시설(BGP)의 일별 운전데이터로 **메탄생성량 예측**과 **소화조 건강상태
관제**를 수행하는 프로젝트.

## 문서 우선순위

작업 전 아래 순서로 읽는다. **뒤 문서가 앞 문서를 뒤집는 구조**이므로 순서가 중요하다.

| 순위 | 문서 | 역할 |
|:---:|---|---|
| 1 | `docs/영천BGP_모델링최종명세_AI인계용.md` | **최신 모델링 결론.** 모델 비교 전수·채택 모델(1차 가수분해)·3층 구조·미해결 O1~O6. 이전 판정 다수를 철회 |
| 2 | `docs/EXPERT_REVIEW.md` | 설계 v2에 대한 독립 검토. 치명 결함(A1·A2) + 신규 정량 기반(B1~B4) |
| 3 | `docs/MODELING_OVERVIEW.md` | 설계 문서 v2. 검토서·명세서가 뒤집은 항목이 많으므로 단독 인용 금지 |
| 4 | `docs/reference/영천BGP_AI참조문서.md` | 스키마·DQ-01~12·정량 관계식의 1차 출처 |
| 5 | `docs/reference/영천BGP_유기물처리계통설명서.md` | 공정 계통·설계 제원·설계 대비 실적 |
| 6 | `docs/INGESTION_REPORT.md` | 위 문서 전부의 재현 검증 결과 + 문서에 없는 재현 규약 + **충돌 목록** |
| 7 | `docs/reference/literature/` | 1차 문헌 4종 (SAO 경로, AD 리뷰 3종) |

**문서 간 충돌이 미해소 상태다.** `y_lag1` 사용 여부, 예측 지평(당일 vs h=7·14), VFA/Alk·
`acid_pH` 피처 채택 여부에서 문서들이 서로 반대를 지시한다. 전체 목록은
`docs/INGESTION_REPORT.md` §8. **모델링 착수 시 이 표부터 확인하고 용도를 확정할 것.**

미편입 선행 문서 2종(`영천BGP_데이터처리기록_및_원본부속정보.md`,
`영천BGP_2018-2023_데이터취급방법론.md`)이 명세서에서 참조된다. 연도별 취급 규칙의
상세 근거이므로 필요 시 확보를 요청할 것.

## 데이터

**정본: `data/영천BGP_MASTER_2018-2023.csv`** (2,086행 × 61열, 2018-01-01 ~ 2023-09-17,
A/B 계열 분리, DQ 플래그 반영).

`data/master.xlsx`·`data/targets.xlsx`는 **구 데이터**다. A/B를 평균으로 합쳐 데이터 결함을
정상 신호처럼 보이게 만든다(설계 §2.1). 신규 작업에 쓰지 말 것.

데이터를 건드린 뒤에는 `python scripts/validate_master.py`(68 검사)를 돌려 회귀를 확인한다.

## 반드시 지킬 규칙

이 규칙들은 전부 데이터로 검증됐다(`docs/INGESTION_REPORT.md`). 어기면 결과가 조용히 틀린다.

1. **persistence를 항상 병기하되, `y_lag1`을 피처로 쓰지 않는다.** 바이오가스는
   ACF(1)=0.916의 확률보행에 가깝고, 이는 HRT 40.5일(일 교체율 2.47%)의 반응조 관성이
   만든 것이다. 원계열 persistence R²=0.831은 **차분계열로 계산하면 −0.000** 이다.
   `y_lag1`을 넣으면 모델이 공정이 아니라 자기 자신을 설명한다(명세서 W1, Granger-Newbold
   허위회귀와 동일 구조). persistence는 **반드시 넘어야 할 기준선**이지 피처가 아니다.
2. **차분계열 R²와 전체평균 모델 R²를 함께 보고한다.** 원계열 R² 단독은 허위성을 감춘다.
3. **무작위 K-fold 금지.** 인접일 누출로 R²가 과대평가된다. 시간순 3분할
   (학습 2018~2021 / valid 2022 / test 2023-09-17)을 쓴다.
   `df.dropna()` 전면 적용도 금지 — 2023 전체와 모든 주말이 날아가 평일 편향이 된다.
   `dropna(subset=[...])`로 필수 컬럼만 지정한다.
4. **주말 랩 데이터 선형보간 금지.** 랩 항목 주말 측정률 0.3%(주중 88%)로 MCAR이 아니다.
   결측 마스크를 피처로 보존한다. 유량 항목(반입·투입·가스)은 요일 무관 ~100%로 성격이 다르다.
5. **A/B 반응조를 독립 표본으로 취급 금지.** r=0.927의 병렬 동일조건 운전이다.
6. **FAN 기반 실시간 경보를 만들지 않는다.** `FAN>300`은 측정일의 75.1%에서 발화하고(비순치
   임계를 순치된 계에 적용), 일별 해상도에서는 성능과 무관하다(r=+0.056, p=0.399 — 연 단위와
   **부호가 뒤집힘**). 암모니아 저해는 만성 현상이지 일별 변동 요인이 아니다. FAN은 2차 원인
   판별 변수로만 쓴다. 대안 경보는 검토서의 `Y_COD < 90일 기준선 −15%`(발화율 9.3%)이나,
   명세서는 이를 평가하지 않았으므로 채택 전 검증이 필요하다.
7. **VFA/알칼리도는 관리지표에서 제외한다.** 암모니아가 NH₄HCO₃로 분모(알칼리도)를
   부풀려, 성능이 하락하는 동안 지표는 "개선"으로 표시된다(2022년 지표 최저 = 메탄 최저).
   Layer 1 잔차와의 상관도 r=0.042로 무의미하다(명세서 Layer 2).
8. **소화조 내부 이화학으로 가스량을 예측하지 않는다.** 소화조 pH CV=1.68%·온도 CV=2.33%로
   변동 자체가 없는데 가스는 CV=23.7%다. 내부 변수 추가 시 test R²가 오히려 하락한다
   (0.688→0.662). 내부 변수는 **예측이 아니라 진단 계층(Layer 2)** 에 배치한다.
9. **VS 기반 지표 대신 `in_TS`(`acid_TS_pct`)를 쓴다.** VS/TS가 2020년에 0.80→0.70으로
   계단 하락해 절대값을 신뢰할 수 없다. 단 **원인은 미상이다** — "측정법 변경"은 근거 없는
   추론이며(명세서 O4 정정), TS도 함께 하락해 기질 변화 가능성을 배제할 수 없다.
   기술할 때는 "2020년 불연속 관측됨, 원인 미상"으로 쓴다.
10. **부하는 질량 기준 `TS_load = feed_AB_tpd × acid_TS_pct / 100`.** 부피(feed 단독)보다
    상관이 높다(r 0.54 → 0.61). 반입량은 여액저장조에서 완전 평활화되어 r≈0.138,
    기질별 반입량은 r≈0.00 — 모두 제외한다.
11. **피처 제외**: `y_lag1`(W1), `acid_pH`(**철회** — valid에선 유용했으나 test에서
    0.784→0.574 붕괴, 연도간 불안정), `in_VS`/`acid_VS_pct`(2020년 불연속),
    `dig_T_A/B_C`(단독 R²=0.001·센서결함), 기질별·전체 반입량, H₂S(고정 가정치),
    개별 VFA 6종(6년간 미측정), lag 12~15일(차분 후 소멸).
12. **채택 판정 규칙: 검증 R² 개선이 0.01 미만이면 채택하지 않는다.** 기계론 모델 전환
    이득이 +0.008, 2018년 전체 배제 이득이 +0.0011에 그친다. 복잡도를 정당화하지 못한다.

## 계산 규약 (문서에 없어 확정한 것)

```python
# Y_COD — 소화조 COD 는 A·B 평균. A 단독이면 검토서 수치와 어긋난다.
dig_cod = df[["dig_CODcr_A_mgL", "dig_CODcr_B_mgL"]].mean(axis=1)
Y_COD = df.CH4_m3d / (df.feed_AB_tpd * (df.acid_CODcr_mgL - dig_cod) / 1000)

# 연도별 집계·상관 전에 열역학 필터를 먼저 적용한다 (검토서 B3).
valid = df[(Y_COD > 0) & (Y_COD <= 0.35)]

# FAN 재계산 시 온도는 '실측, 센서결함이면 38.0'. 항상 38 대입하면 최대 141 mg/L 어긋난다.
T = df.dig_T_A_C.where(df.flag_TA_sensor_fault == 0).fillna(38.0)
```

- **2018년은 Y_COD 기준선 산출에서 제외한다** — 유효 관측 50건 중 72%가 열역학적으로 불가능.
- **DQ 플래그 구간은 이미 NaN 마스킹돼 있다.** 설계 §3 파이프라인 ① 단계는 무연산이며,
  플래그 열은 "이 결측의 사유 코드"로 쓴다.

## 채택 모델 (명세서 §1 — 아직 미구현)

```
biogas(t) = a · H(t; k) + c        H = 1차 가수분해 커널, k=0.35 /d, 파라미터 3개
```

test R²=0.776 (k=0.45), 0.772 (k=0.35). ADM1-R4 축약(0.778, 파라미터 6개)과 동등하나
절반의 복잡도이고 `k_hyd`가 물리적으로 해석 가능해 채택됐다. **k의 95% CI는 [0.15, 1.50]으로
넓으니 점추정 0.35를 단정적으로 인용하지 말 것.** 재현 코드는 명세서 §9.

## 현행 코드 상태

`src/` 는 위 규칙 이전(v1)에 작성되어 규칙 1·3·7·8·9·11을 위반한다
(상세: `docs/INGESTION_REPORT.md` §5). 아직 개편하지 않았다.

**모델링 착수 전 반드시 `docs/INGESTION_REPORT.md` §8(문서 간 충돌 목록)을 확인할 것.**
특히 예측 지평이 미해소다 — 검토서는 h=7·14일(운영 의사결정 단위), 명세서는 당일 기질기반
예측(부하→가스 응답이 0~3일에 집중)을 지시한다. 서로 다른 용도이므로 무엇을 만들 것인지
먼저 확정해야 한다.

## 실행

```bash
pip install -r requirements.txt
python scripts/validate_master.py   # 데이터셋 수용 검증 (68 검사)
python -m src.train                 # v1 파이프라인 (구 데이터 기준, 개편 대상)
```
