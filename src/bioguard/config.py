"""
전역 상수 — PROMPT §12 요구사항: V·SRT·k·체류시간을 모두 최상단 상수로 노출한다.

여기 있는 값은 전부 **외생 입력**이다. 특히 V(소화조 유효용적)와 그로부터 나오는 SRT는
데이터로 추정되지 않는다(PROMPT §5.2 — 여섯 경로 전부 실패). 바꾸려면 이 파일만 고친다.
"""

from __future__ import annotations

SEED = 20240817

# ---------------------------------------------------------------------------
# 소화조 용적과 SRT — PROMPT §2.3
#   SRT = V / Q_out,  Q_out ≈ Q_in ≈ 197 t/d (밀도비 1, SRT=HRT 가정)
#   SRT 를 추정 모수로 쓰지 말 것. 아래 세 값 전부에 대해 민감도를 낸다.
# ---------------------------------------------------------------------------
V_DIGESTER_M3 = 8000.0                      # 설계값. 운전 실적 확보 시 교체
V_SENSITIVITY = (4000.0, 6000.0, 8000.0)    # → SRT 20.3 / 30.5 / 40.6 일
SRT_REFERENCE_D = 25.0                      # 선행 분석 기준값 (비교용 고정)

# ---------------------------------------------------------------------------
# 기질별 겉보기 분해속도 k [1/d]
#   PROMPT §10: 음폐수 k=0.08 가정은 근거가 약하다(5:5 혼합 조건값). 0.2~0.35 재설정 후
#   민감도 분석하라는 지시에 따라 기본값을 0.30 으로 두고 격자로 훑는다.
# ---------------------------------------------------------------------------
K_HYD = {"foodww": 0.30, "manure": 0.08, "food": 0.30}
K_GRID = {                                   # 민감도 격자
    "foodww": (0.20, 0.30, 0.35, 0.40),
    "manure": (0.05, 0.08, 0.12),
    "food": (0.20, 0.30, 0.40),
}

# ---------------------------------------------------------------------------
# 전단 체류시간 [일] — PROMPT §2.1 / frontend_lit.py 의 문헌 합성값
#   (최소, 표준, 최대). 기계전처리·스트레이너·협잡물처리기는 분 단위라 순수 지연.
# ---------------------------------------------------------------------------
STAGE_TAU = {
    "저장호퍼":      (2.0, 2.5, 3.0),      # 음식물류 전용. 이동진(2015) 최소 2일·권장 3일
    "기계전처리":    (0.007, 0.015, 0.03),  # 파봉파쇄~여액분리 합 10~30분
    "스트레이너":    (0.002, 0.005, 0.01),  # 음폐수 전용, 초 단위
    "협잡물종합처리기": (0.005, 0.01, 0.02),  # 가축분뇨 전용, 분 단위
    "여액저장조":    (2.0, 2.5, 3.0),      # 합류 지점. 지침 최소 2일·권장 3일
    "유기산발효조":  (2.0, 3.0, 4.0),      # 국내 전규모 3일(SL공사), 국제 2~3일
}
STAGE_MODEL = {                              # CSTR = 지수 RTD, delay = 순수 지연
    "저장호퍼": "CSTR", "여액저장조": "CSTR", "유기산발효조": "CSTR",
    "기계전처리": "delay", "스트레이너": "delay", "협잡물종합처리기": "delay",
}
# 합류 이전 = 기질별 개별 경로, 이후 = 공통 (PROMPT §2.1, 금지사항 7)
PATH_INDIVIDUAL = {
    "food":   ["저장호퍼", "기계전처리"],
    "foodww": ["스트레이너"],
    "manure": ["협잡물종합처리기"],
}
PATH_COMMON = ["여액저장조", "유기산발효조"]
SCENARIOS = {"최소": 0, "표준": 1, "최대": 2}
LEGACY_TAU_MIX_D = 3.0                       # 기존 가정: 전 기질 공통 τ_mix=3일 단일 커널

SUBSTRATES = ("foodww", "manure", "food")
SUB_KR = {"foodww": "음폐수", "manure": "가축분뇨", "food": "음식물"}

# ---------------------------------------------------------------------------
# 기질 성상 — PROMPT §3 T1 (Buswell 화학양론 입력). **전부 문헌 가정치다.**
#   carb/prot/lipid 는 VS 기준 질량분율, TSfrac 은 습중량 기준 TS 비율.
#   출처는 선행 리포트(docs/prior_reports/영천BGP_혐기소화_공정관제_리포트.html)가
#   정리한 문헌값을 승계했다.
# ---------------------------------------------------------------------------
SUB_COMPOSITION = {
    "foodww": {"carb": 0.62, "prot": 0.16, "lipid": 0.22, "BD": 0.82,
               "TSfrac": 0.055, "VSTS": 0.88,
               "src": "음식물류 재활용시설 침출수 BMP(28일 478 mLCH₄/gVS), 가용성 분획 우세"},
    "manure": {"carb": 0.52, "prot": 0.24, "lipid": 0.07, "BD": 0.45,
               "TSfrac": 0.045, "VSTS": 0.75,
               "src": "돈분·우분 슬러리. 장내 소화 후 난분해성 섬유질 잔존, BD 0.4~0.5"},
    "food":   {"carb": 0.58, "prot": 0.18, "lipid": 0.24, "BD": 0.69,
               "TSfrac": 0.18, "VSTS": 0.90,
               "src": "국내 음식물류 BMP 실측 0.36 / 이론 0.52 Sm³CH₄/kgVS → BD 0.69"},
}
# Buswell 용 대표 분자식 (탄수화물·단백질·지질)
BUSWELL_COMP = {
    "carb":  {"formula": "C6H10O5",   "c": 6,  "h": 10,  "o": 5, "n": 0, "Mw": 162.14},
    "prot":  {"formula": "C5H7O2N",   "c": 5,  "h": 7,   "o": 2, "n": 1, "Mw": 113.12},
    "lipid": {"formula": "C57H104O6", "c": 57, "h": 104, "o": 6, "n": 0, "Mw": 885.45},
}
MOLAR_VOLUME_L = 22.414          # 표준상태 ㎥/kmol
COD_TO_CH4_M3_PER_KG = 0.35      # 이론 최대 ㎥CH₄/kg COD 제거
COD_PER_VS_KG = 1.42             # kg COD / kg VS

# ---------------------------------------------------------------------------
# 물리 정합성 상한 — PROMPT §9 / §10
# ---------------------------------------------------------------------------
YIELD_CAP_VS_IN = {"food": 0.65, "foodww": 0.65, "manure": 0.48}   # ㎥CH₄/kgVS_in
YIELD_CAP_VS_DESTROYED = 0.50                                      # 이론 최대
COD_CAP_KG_PER_T = {"foodww": 118.3, "manure": 77.2, "food": None}  # None = 식별 불가
INTERCEPT_WARN_FRAC = 0.15        # 절편이 실측 평균의 15% 초과 → T1 단독 예측 중단 신호

# ---------------------------------------------------------------------------
# 검증 프로토콜 — PROMPT §8
# ---------------------------------------------------------------------------
CV_INITIAL_TRAIN_D = 365
CV_HORIZON_D = 90
CV_STEP_D = 90
HOLDOUT_START = "2023-01-01"      # 최종 1회 보고용. 모델 선택에 쓰지 않는다.
SPLIT_TRAIN_END = "2021-12-31"
SPLIT_VALID_END = "2022-12-31"

# 선행 분석이 보고한 기준선 (PROMPT §5.5). 우리 타깃 계열이 다르면 재현되지 않을 수 있으므로
# 값 자체를 목표로 삼지 말고, 동일 폴드에서 우리가 다시 만든 기준선과 비교한다.
REPORTED_BASELINES = {
    "기존전단_무절편": 946.6, "기존전단_절편": 884.1,
    "문헌전단_무절편": 905.8, "문헌전단_절편": 861.5,
}

# 건강상태 경보 임계 — PROMPT §6
HEALTH_THRESHOLDS = {
    "VFA_ALK":  {"warn": 0.3,  "crit": 0.4},
    "pH":       {"lo": 6.8,    "hi": 7.6},
    "OLR":      {"lo": 1.5,    "hi": 4.0},
    "FAN":      {"warn": 150.0, "crit": 300.0},
    "TEMP":     {"set": 38.0,  "tol": 1.0},
}
